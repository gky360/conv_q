<div class="page-header">
  <h1>
    <%= @user.name %>
    <small>
      @<%= @user.account %>
      <span><%= @histories_count[:today] %></span>
      <span class="histories_count_split">/</span>
      <span><%= @histories_count[:total] %></span>
    </small>
  </h1>
</div>

<dl class="dl-horizontal">
  <dt>Name:</dt>
  <dd><%= @user.name %></dd>
  <dt>Account:</dt>
  <dd>@<%= @user.account %></dd>
  <dt>Done topics:</dt>
  <dd>
    Today: <span><%= @histories_count[:today] %></span> / Total: <span><%= @histories_count[:total] %></span>
  </dd>
</dl>

<% if user_signed_in? && @user.id === current_user.id %>
<div>
  <p>
    <%= link_to edit_user_registration_path, class: "waves-effect waves-light btn" do %>
    <i class="material-icons">settings</i> Settings
    <% end %>
  </p>
</div>
<% end %>

<div>
  <p>
    <%= link_to "Histories", user_histories_path(@user.account), class: "waves-effect waves-light btn light-green lighten-1" %>
    <%= link_to "Topics", user_topics_path(@user.account), class: "waves-effect waves-light btn light-green lighten-1" %>
    <%= link_to user_reports_path(@user.account), class: "waves-effect waves-light btn-flat grey-text text-darken-2" do %>
      <i class="material-icons">report_problem</i> Reports
    <% end %>
  </p>
</div>
