<div class="page-header">
  <h1><%=t '.title', :default => Topic.model_name.human.titleize %></h1>
</div>

<%= render partial: "topic_show", locals: { topic: @topic } %>
<%= render partial: "topic_timer", locals: { topic: @topic, next_topic: @next_topic } %>

<p>
  <%= link_to t('.back', :default => t("helpers.links.back")),
                topics_path, :class => 'waves-effect waves-light btn'  %>
  <% if @topic.by_user?(current_user) %>
  <%= link_to "Edit", edit_topic_path(@topic), :class => 'waves-effect waves-light btn light-green lighten-1' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                topic_path(@topic),
                :method => 'delete',
                :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                :class => 'waves-effect waves-light btn red lighten-1'
  %>
  <% end %>
</p>
